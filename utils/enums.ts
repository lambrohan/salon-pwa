export enum SalonServiceStatus {
  ON = 'ON',
  OFF = 'OFF',
}

export enum UserGender {
  MALE = 'MALE',
  FEMALE = 'FEMALE',
  OTHER = 'OTHER',
  RATHER_NOT_SAY = 'RATHER NOT SAY',
}

export enum AppointmentStatus {
  ONGOING = 'ongoing',
  FINISHED = 'finieshed',
  CREATED = 'created',
}

export enum AppointmentTypes {
  ONLINE = 'online',
  WALKIN = 'walkin',
}

export enum SalonRoles {
  STYLIST = 'salon_stylist',
  OWNER = 'salon_owner',
}
